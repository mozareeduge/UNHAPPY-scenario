# Repository instructions

- The current explicit user request and any project-specific execution package supplied in the active session govern the bounded task.
- No persistent repository workload is active; do not infer one from Git history or old reports.
- Preserve authored text, rights, privacy/offline behavior, tests, and unrelated work unless current authority explicitly changes them.
- Do not create or modify `.claude/`, Claude settings, hooks, skills, agents, plugins, MCP, permission profiles, `.git/info/exclude`, global Git configuration, or files outside this repository.
- Keep every change visible in `git status` and bind completion evidence to the unchanged candidate.
