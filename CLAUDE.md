# Repository instructions

Read `@PROJECT_RELAY.md` before doing project work.

Do not create or modify `.claude/`, Claude settings, hooks, permission profiles, `.git/info/exclude`,
global Git configuration, or files outside this repository. Keep every change visible in `git status`.
Current explicit user instructions and the visible active workload outrank generic Relay procedure.
